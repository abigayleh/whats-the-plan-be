node_modules
.env
dist/
uploads/
*.log
scripts/seed-demo-manifest.json

# Claude Code: the marker files and agent memory are shared; the rest is machine-local.
.claude/settings.local.json
.claude/worktrees/
